// No wildcard permit is allowed in the repository policy baseline.
// After the live AgentCore Gateway schema is captured, add one explicit permit
// per registered remediation action and validate it with the AgentCore analyzer.
// The permit set is inactive until an audited control-plane activation.
